#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../testem-multi/bin/testem-multi" "$@"
  ret=$?
else 
  node  "$basedir/../testem-multi/bin/testem-multi" "$@"
  ret=$?
fi
exit $ret
